name: 'analyse'

on:
  pull_request:
  push:
    branches:
      - main
      - 'releases/*'

jobs:
  psalm:
    runs-on: php:8.2
    steps:
      - uses: actions/checkout@v2
      - run: |
          composer install
      - run: |
          composer run psalm